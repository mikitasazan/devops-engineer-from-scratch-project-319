apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: bulletin-board
  namespace: bulletin-board
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: bulletin-board
  minReplicas: 2
  maxReplicas: 4
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
