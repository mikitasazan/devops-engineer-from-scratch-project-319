apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bulletin-board.fullname" . }}-config
data:
  SPRING_PROFILES_ACTIVE: {{ .Values.config.springProfilesActive | quote }}
  MANAGEMENT_SERVER_PORT: {{ .Values.config.managementServerPort | quote }}
